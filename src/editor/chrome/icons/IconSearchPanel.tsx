/* DEPRECATED — kept as an empty stub because the file can't be deleted from
 * the mount. Icon search now lives inline inside LibraryPanel and
 * MoreShapesPopover via IconSearchResults. Safe to delete this file from
 * disk on the host.
 *
 * If anything still imports IconSearchPanel, the import will fail at build —
 * which is the right behaviour, it's a signal to update the import site. */
export {};
